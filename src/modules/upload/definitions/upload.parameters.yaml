components:
  parameters:
    UploadId:
      name: id
      description: Upload's Id
      in: path
      required: true
      schema:
        $ref: "../../../../node_modules/@yest/contract/resources.yaml#/components/schemas/ObjectID"
    IsUploadDryRun:
        name: isDryRun
        description: Should this request be ran as a dry run (no save)
        in: query
        required: false
        schema:
          type: boolean
    IsUploadArchived:
      name: isArchived
      description: Get archived Upload
      in: query
      required: false
      schema:
        type: boolean
